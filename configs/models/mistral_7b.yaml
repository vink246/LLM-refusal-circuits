# Model configuration for Mistral-7B-Instruct-v0.1

model:
  name: "mistralai/Mistral-7B-Instruct-v0.1"
  device: "cuda"  # or "cpu"
  torch_dtype: "float16"  # "float16" or "float32"
  trust_remote_code: false
  cache_dir: "/home/hice1/mchen692/scratch/huggingface"  # Optional: HuggingFace cache directory (e.g., "/path/to/hf_cache" or "~/.cache/huggingface")
  
  # Layers to collect activations from
  # Format: residuals_N, mlp_N, or attention_N where N is layer index
  activation_layers:
    - "residuals_21"
    - "residuals_22"
    - "residuals_23"
    - "residuals_24"
    - "residuals_25"
    - "residuals_26"
    - "residuals_27"
    - "residuals_28"
    - "residuals_29"
    - "residuals_30"
    - "residuals_31"
    # Add more layers as needed

inference:
  batch_size: 4  # Adjust based on GPU memory
  max_new_tokens: 20  # Number of tokens to generate
  save_activations: true

data:
  # Data configuration (can be overridden by --data-config)
  path: "data/raw/or-bench"
  categories: []  # Empty = all available categories
  category_balance_strategy: "use_all"  # "use_all" or "equalize"

output:
  result_dir: "results"

